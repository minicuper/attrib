index(bem='Input' attributes='label description')
  view(is='{{ _layout }}' inherit)

pure:
  view.-pure(is='input' extraClass='{{ @class }}' inherit)

columns:(bem='Input')
  .&.-columns.-datetimeNative(class="{{if @class}}{{@class}}{{/}}{{if errors}}-error{{/}}")
    view(is='inputErrors')
    .&-left
      if @label
        label.&-label {{ @label }}
    .&-right
      view(is='input' inherit)
      if @description
        .&-description {{ @description }}

input:(bem='Input')
  if @readonly
    view(is='input:readonly' inherit)
  else
    view(is='input:edit' inherit)

input:readonly:(bem='Input')
  .&-input.-datetimeNative.-readonly(class='{{ @extraClass }}')
    | {{ value || '&nbsp;' }}

input:edit:(bem='Input')
  input.&-input.-datetimeNative(class='{{ @extraClass }}'
      type='datetime-local'
      value='{{ value }}'
      min='{{ @min }}'
      max='{{ @max }}'
      disabled='{{ @disabled }}'
      on-focus="emit('active')"
      as='input'
  )
