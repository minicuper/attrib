index(bem='Input')
  view(is='{{ _layout }}' inherit)

pure:
  view.-pure(is='input' extraClass='{{ @class }}' inherit)

columns:(bem='Input')
  .&.-columns.-password(class="{{if @class}}{{@class}}{{/}}{{if errors}}-error{{/}}")
    view(is='inputErrors')
    .&-left
      if @label
        label.&-label {{ @label }}
    .&-right
      view(is='input' inherit)
      if @description
        .&-description {{ @description }}

input:(bem='Input')
  if @readonly
    view(is='input:readonly' inherit)
  else
    view(is='input:edit' inherit)

input:readonly:(bem='Input')
  .&-input.-password.-readonly(class='{{ @extraClass }}')
    | {{ '••••••' || '&nbsp;' }}

input:edit:(bem='Input')
  input.&-input.-password(class='{{ @extraClass }}'
      type='password'
      value='{{ _value }}'
      disabled='{{ @disabled }}'
      placeholder="{{ @placeholder || 'write here' }}"
      on-blur='lazySet()'
      on-focus="emit('active')"
      as='input'
  )
