index
  view(is='_index' inherit)

_index(bem='InputRadioLabels')
  .&(class="{{if @class}}{{@class}}{{/}}")
    .&-left
    .&-right
      each @options as #option
        .&-item
          .&-transformBox(class="{{ @rotate && '-rotate' }}")
            | {{ #option.name || #option }}
