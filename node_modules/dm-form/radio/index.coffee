FormSelect = require '../select'

module.exports = class FormRadio extends FormSelect
  view: __dirname
  style: __dirname
  name: 'dm-form:radio'

  _getExtraFields: ->
    super().concat [
      'compact' # Compact view of showing radio as select
      'horizontal'
    ]
